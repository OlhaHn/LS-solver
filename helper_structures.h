#ifndef HELPER_STRUCTURES_H
#define HELPER_STRUCTURES_H

#include "includes.h"

/*
Map: (a, b) where b = 2^a or b = 5^a depending on selected diff heurisitic
*/
std::unordered_map<int, double> powers = std::unordered_map<int, double>();

#endif